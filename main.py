import os

if __name__ == "__main__":
    print("[SYSTEM]: Bootstrapping Smart Assist Dashboard Engine...")
    # Executes the command line wrapper to launch the web pipeline layout
    os.system("streamlit run app.py")
